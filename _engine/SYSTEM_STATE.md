# 시스템 인수인계 메모 (역사적 기록)

> ⚠️ **새 주제로 복제·확장하려면 먼저 `HANDOFF_홈판블로그_복제가이드.md`를 읽으세요.**
> 그 문서가 현재(v3: 프리미엄 사진 3API + photo_category 자동선택 엔진 + 5장 구성 + 안정성
> 하드닝) 기준의 종합 복제 가이드입니다. 이 파일(SYSTEM_STATE)은 그 이전(v1/v2 전환기)
> 진행 이력과 페르소나 v2 스펙을 담은 **역사적 기록**으로, 세부는 여전히 유효하지만
> "output 4장 / Wikimedia·Openverse만 / v2 테스트 중" 같은 서술은 v3 이전 상태입니다.

최종 업데이트: 2026-08-02

## 한 줄 요약
네이버 홈판 경제 블로그(브랜드: 경제비버) 자동 생성 시스템. 매일 새벽 클라우드 루틴이
기사→본문+이미지4장을 만들어 이 저장소 output/ 에 커밋. 사용자는 GitHub Desktop으로
받아 네이버에 복붙 발행. (자동 발행은 네이버 정책상 불가 — 복붙까지가 자동화 범위)

## 지금 상태: 시스템 구축 완료, 실제 발행 검증 대기
- 코드/규칙/이미지 파이프라인 전부 완성. 규격 위반 0으로 빌드됨.
- 아직 사용자가 네이버에 실제 1회 발행은 안 해봄 (다음 할 일).

## 폴더 구조
```
output/{MMDD}/{순번}/   ← 사용자가 매일 보는 결과물
   0번 본문.txt          붙여넣기용 (제목+본문, 점자빈칸 문단간격)
   1~4번 사진.jpg        1080px JPG, 【N번 사진】 마커 자리에 삽입
_engine/                ← 코드·규칙 (사용자는 안 봄)
   build_posts.py        기사JSON → output 생성 (핵심)
   common_utils.py       이미지 카드 SVG 빌더들
   image_sourcing.py     Wikimedia/Openverse 이미지 소싱
   fetch_logo/photo/portrait.py, build_photo/ticker_library.py  자산 수집 CLI
   PROMPT_V1.md          경제 주제 생성 규칙 (제목/본문/썸네일/반응장치/가독성/스키마)
   PROMPT_AUTO_V1.md     자동차 주제 규칙 (보류 상태)
   articles_test.json    시연용 기사 1건
assets/                 ← 이미지 재료 (로고 22종/사진/인물, 자동 수집)
state/                  ← 중복방지 기록 seen_articles.json
```

## 빌드/실행 (반드시 저장소 루트에서)
```
python _engine/build_posts.py _engine/articles.json 0802
```
→ output/0802/{순번}/ 에 0번 본문.txt + N번 사진.jpg 생성. build_report.json에 검증 결과.

## 확정된 규칙 (사용자 피드백으로 굳은 것 — 함부로 바꾸지 말 것)
- 문장 중간 줄바꿈 금지. 문단(2~3문장) = 한 줄, 네이버가 감쌈. 110자 넘으면 문장경계서 빈줄.
- 소제목 📌 밑에 구분선 ━×19 자동. 콜아웃 💡(핵심/관점)·👉는 독립문단.
- 여운형 질문은 글 맨끝(정리 다음, 면책 앞). '댓글' 단어 절대 금지, 구걸 금지.
- 면책 짧게: "※ 본 게시글은 투자 권유가 아니며, 투자 판단과 책임은 본인에게 있습니다."
- 이미지 4장은 빌더가 글자수 균등 배치 (body에 마커 안 넣어도 됨). 마지막은 정리카드=맨끝.
- ⭐ images 1번 = 반드시 '홈판 대표 썸네일'. 종목 등락=stock_thumbnail(곤두박질/급등 차트),
  그 외=thumbnail(사진배경+2줄 스레드체 카피+민트강조+경제비버 배지). 데이터카드로 만들지 말 것!
  2~4번만 bar/rank/summary/number/photo_card. (PROMPT_V1.md 7절 참고)
- 이미지 = 1080px JPG q92 (폴더 540KB). 종목기사 썸네일은 한국식 색(상승 빨강/하락 파랑).
- 볼드마킹(▶)은 폐기 (네이버 미적용). 강조는 이모지+카드가 담당.

## 이미지 종류 (common_utils.py)
photo_card(사진배경+숫자) / bar_card(막대) / rank_card(순위) / summary_card(정리3줄)
/ number_card(숫자카드) / thumbnail(사진2줄카피) / stock_thumbnail(주가 곤두박질/급등 차트)

## 이미지 소싱 한계 (실측 확정)
- Wikimedia Commons 직접 = 고해상 보장, 채택. Openverse = 축소본 많아 실측검증 필수.
- Unsplash/Freepik/Pexels = 차단(401/403). korea.kr = 720px 상한.
- 감정 스톡사진(슬픈 직장인 등) 무료로 거의 없음 → 종목은 차트 직접 생성, 생활감정은 사물 대체.
- 로고/인물은 상표·초상. 재게시 가능 라이선스(PD/CC/KOGL)만, 반드시 육안 검수.
- Openverse 검색어는 짧게(1~2단어)! 3단어 이상 조합하면 결과가 0건으로 잘리는 경우가 잦다
  (예: "bitcoin coin close up photo"=0건, "bitcoin coin"=정상). 안 나오면 단어 수부터 줄여본다.
- Wikimedia 썸네일 렌더(`wikimedia_thumb_url`, 임의 width)가 일부 파일에서 HTTP 400
  ("Use thumbnail sizes listed…")을 낸다 — 위키미디어가 임의 폭 렌더를 막기 시작한 파일들.
  `image_sourcing.py`의 `photo_candidates()`에 원본 URL 폴백을 넣어 고침(2026-08-02) —
  이미 원본이 크면(대부분 4000px+) 어차피 빌더가 1080px로 다시 줄이므로 무겁지 않다.
- `fetch_photo.py`의 성공 판정 버그도 같이 고침: "skipped"(축소본이라 스킵) 항목을
  파일이 저장된 것처럼 잘못 세던 것을 `"path" in r` 기준으로 수정(2026-08-02).

## 자동화 루틴 (claude.ai 스케줄)
- 경제: trig_0185N5DzZMWkwwVQFHaNWC1u (활성, 매일 06:00 KST)
- 자동차: trig_017PWa529j3mbdgB8Tq96PtC (비활성/보류)
- 환경: env_01R9XQUDmstnAoB8JoQWebwF (네트워크 무제한). 저장소·프롬프트 연결됨.
- 주의: RemoteTrigger 부분 업데이트가 sources/프롬프트를 지운 적 있음 → update 시 전체 유지.

## 확장 방법 (자동차/건강 등 새 주제)
GitHub 링크 공유 방식으로 무한 확장. 새 주제 = 새 저장소 복제:
1. _engine 코드는 100% 재사용
2. 바꿀 것 4개: 브랜드명(build_posts.py 기본값) / 사진검색어(build_photo_library.py CATEGORIES)
   / 주제규칙(PROMPT) / RSS소스(루틴 프롬프트)
3. 새 저장소 Public → 담당자에게 output 폴더 링크 공유 → 각자 복붙
4. 주제별로 분리돼야 유사문서 회피됨 (같은 공장 티 안 남)

## 2026-08-02 진행 상황 (중요)
- 첫 자동 실행(08-02 06:00) 성공: 경제비버 8건 생성 → output/0802/1~8 에 GitHub push 완료.
  사용자는 GitHub Desktop Fetch → PC 문서/GitHub/economy-blog 폴더에서 받아 발행하는 흐름 셋팅 완료.
- 겪은 이슈들(전부 해결):
  a) 자동화 프롬프트가 옛버전이었음 → _engine 경로/build_posts/0번본문·N번사진.jpg 최신으로 교체.
  b) git push 403 "Resource not accessible by integration" (GitHub App 쓰기권한 부족)
     → 결국 권한 열려 push 성공. 단 GitHub Settings>Installed Apps엔 Claude 없고 Cursor만 보임
     (claude.ai 쪽 연동). 재발 시 claude.ai 저장소 재연결로 쓰기권한 부여.
  c) 알림 채널(email/push/slack) 전부 false → 폰 알림 안 감. 필요시 claude.ai 앱 알림만 켜면 됨.
     (당장은 GitHub Desktop Fetch로 폴더 생겼나 확인 = 알림 대용)

## 최근 반영한 디자인 피드백 (커밋됨)
- images 1번 = 반드시 홈판 대표 썸네일(thumbnail/stock_thumbnail). 데이터카드 금지. (PROMPT 7절)
- 썸네일 브랜드 배지 '경제비버'의 배경 박스(테두리) 제거 → 텍스트만 (외곽선 약하게).
- 모든 카드 이미지에서 하단 출처/자료 표기 제거 (_card_note는 항상 "" 반환, photo credit 무시).

## 경제비버 v2 제작 스펙 (2026-08-02 확정 — 톤/페르소나 대개편, 테스트 진행 중)
v1(위 "확정된 규칙")은 중립적 정보전달자 톤이었다. v2는 여기서 한 걸음 더 나가
"개미 편에서 관점을 던지는" 캐릭터 블로그로 전환한다. 현재는 `output/samples/0802_v2/`에서
샘플로만 검증 중 — 실제 발행(`output/{MMDD}/`)과 자동화 루틴 프롬프트는 아직 v1 그대로다.
v2를 정식 채택하면 루틴 프롬프트·PROMPT_V1.md를 v2 기준으로 다시 정리해야 한다.

1. **노스스타**: 중립 정보전달자는 이미 널렸다. 경제비버는 개미 편에서 관점을 던지는
   유일한 목소리를 지향한다. 정보 나열이 아니라 "이건 이렇게 봐야 한다"는 입장이 있어야 한다.
2. **페르소나(화자)**: 경제비버 = 40대 직장인, 국장에 여러 번 물린 개미. 큰돈 벌어본 적은
   없지만 귀동냥은 많고, 그래도 결국은 잘 되겠다는 열정이 있는 1인칭 캐릭터.
   - 타겟 독자: 40~50대 이상. 친밀감·공감형 어투.
   - 셀프디스는 문, 분석은 방: 손실·과거 베팅 얘기는 자조("나도 물렸다")로 풀고,
     관점·해석을 펼 때는 자신감 있게 단정한다.
   - 구체적인 개인 수익률·손실액을 지어내는 건 금지. "개미면 다 아는 심정" 수준의
     보편적 공감까지만 — 없는 개인 경험을 사실처럼 창작하지 않는다.
3. **편(便)과 빌런**: 경제비버는 언제나 개미 편이다. 분노와 비판은 빌런에게만 겨누고,
   독자나 실존 개인에게는 절대 겨누지 않는다(명예훼손 금지, 공인의 공적 활동 비판은
   기존 톤 규칙과 동일하게 사실 기반·조건부로만).
   - 주식 = 외국인·기관·세력
   - 코인 = 고래·거래소·김치프리미엄
   - 부동산 = 정책·투기꾼
   - 예적금/노후 = 은행·보험사
   - 지원금/정책 = 정부 생색내기·사각지대
4. **감정 로테이션**: 매일 같은 세기로 반복하면 충격이 인플레돼 식상해진다(양산형 회피).
   - 기본(매일): 손실회피 + 연민 + 관점. 강도는 "진라면~신라면" 정도.
   - 스파이크(주 1~2회, 또는 방탄 데이터가 딱 맞아떨어질 때만): 계급·서열 낙인,
     불닭 매운맛 강도.
   - 가끔: 정보 정공법 / 셀프디스 공감형.
5. **제목 공식**: [종목·검색키워드를 앞쪽에] + [감정 훅] + [개미 관점의 뒤 여운].
   가격을 단정 예측하는 표현("내일 X만원 아래로")은 금지 — 반드시 관점으로 리프레임한다
   (기존 "절대 금지" 규칙과 동일선상, 화력은 감정과 여운에서 낸다).
6. **계급/서열 시리즈(스파이크 전용)**: [커트라인 숫자] + [계급 낙인] + [인정하기 싫은 진실]
   구조. 자산/소득/주식/코인/부동산/노후/지원금 등 카테고리별로 만들 수 있다.
   - **방탄 데이터 원칙**: 커트라인 숫자는 반드시 통계청·KB금융·한국은행 같은 공신력 있는
     출처의 실제 통계를 근거로 한다 — 반박당해도 무너지지 않아야 스파이크 톤을 써도 안전하다.
   - 판단은 항상 집단(계층·구간) 단위로만 하고, 실존 개인을 특정해 서열을 매기지 않는다.
7. **말투**: 본문 몸통은 존댓말 장문(SEO·40~50대 신뢰용 문체, 기존 가독성 규칙 그대로
   문장당 60자 내외·문단 2~3문장). 첫 훅·소제목·콜아웃(💡 등)에서만 스레드식 구어로
   스파이크를 준다. 순수 스레드 단문 포맷을 본문 전체에 이식하는 건 저품질로 보여 금지.
8. **이미지 5장 구성** (v1의 4장에서 "실물 이미지" 1장이 추가됨):
   1. 썸네일 — thumbnail 또는 stock_thumbnail만(데이터카드 금지, v1 규칙 그대로).
      후킹 카피 2줄.
   2. 실물 이미지 — 실존 인물 사진, 또는 종목이면 로고·본사·현장 사진.
      저작권·초상권은 금지가 아니라 라이선스 기준으로 판단해서 실제로 소싱한다
      (`economy-image-sourcing` 스킬 / `_engine/fetch_photo.py`, `fetch_portrait.py`,
      `fetch_logo.py` 사용, `assets/photos|portraits|logos|tickers/index.json`에 기록).
   3~5. 카드 3장 — 데이터 카드(bar/number/rank 중 필요한 조합) → 마지막은 summary_card로
      정리. (v1 규칙 그대로 유지: 카드 출처표기 텍스트 없음, 배지 배경박스 없음,
      1080px JPG q92, 문단·구분선·콜아웃·여운형 질문·면책 규칙 유지.)
9. **네이버 홈판 레버**: 첫 3줄(더보기 접히기 전)에 훅을 배치. 마지막 여운형 질문은
   찬반이 갈릴 만한 질문으로 던져 댓글을 유도한다('댓글' 단어 자체는 여전히 금지).
   06:00 발행 유지, 매일 꾸준히 발행. 저품질 회피 = 제목은 맵게 쓰되 본문은 방탄
   데이터로 반드시 뒷받침한다(payoff 필수, 낚시성 이탈 방지 원칙은 v1과 동일).
10. **채널 분리 (기록만, 이번 생성물은 네이버 하이브리드용)**: 스레드는 진짜 반말 단문
    유입퍼널, 네이버는 존댓말 장문 + 스레드식 스파이크를 섞은 하이브리드. 지금 파이프라인은
    네이버용만 만든다 — 스레드 전용 포맷은 아직 미착수.

## ✅ 썸네일 카피 가독성 — 해결 완료 (2026-08-03)
문제였던 것: 카피가 길면 폰트가 80px까지 깎여 모바일 홈판에서 안 읽힘.
확정 원칙: **"폰트를 줄이지 말고 줄을 늘려라."**

구현 = `common_utils.layout_thumb_copy(line1, line2)` → `(줄 리스트, 폰트크기)`
  1. 작성자가 끊어준 2줄이 86px(COPY_PREFER_FS) 이상이면 그대로 존중
  2. 안 되면 어절 단위로 2줄→3줄→(최후)4줄까지 늘리며 큰 폰트를 찾는다
     — 분할 우선순위: 폰트 크기 > 작성자가 끊은 자리 > 줄 길이 균형
  3. 폰트 하한 76px(COPY_MIN_FS) 절대 보장, 짧은 카피는 118px(COPY_MAX_FS) 초대형
실측 결과: 6~10자 → 118px / 11~12자 → 106~108px / 긴 카피 → 3줄 90px (2줄 80px 대비 크게 개선)

폭 추정(`_est_text_width`) 함정 2가지 — 실측으로 잡음, 건드릴 때 주의:
  - '%'는 좁은 기호가 아니라 사실상 전각(0.95). 좁게 잡으면 '41%는~'이 오른쪽으로 넘친다
  - 글자 외곽선(fs*0.17)이 좌우로 삐져나오므로 그 몫을 폭에 더해야 한다
프롬프트도 함께 조임: PROMPT_V1.md 3-1절·7절 → 한 줄 6~10자(최대 11자).
  "3줄 자동전환은 안전장치일 뿐, 길게 써도 되는 핑계가 아니다"까지 명시.
미리보기 도구: `python _engine/preview_thumbs.py` → output/preview_thumb/ 에 짧/중/긴/종목 5종 렌더

## ✅ 썸네일 기업 로고 — 도입 완료 (2026-08-03)
사용자 지적: "하이닉스 폭락왔다 썸네일 이미지 매칭 최악. 회사 브랜드 로고 건물
적극적으로 가져와서 이미지 퀄리티 올려."
현실: Commons에 한국 기업 사옥 사진이 거의 없다(SK하이닉스=메모리 제품뿐,
네이버·삼성·거래소=0건). 그래서 **로고로 푸는 게 정답**.

- `build_thumbnail_svg(logo_path=)` → 좌상단 흰 판 배지(우상단 경제비버와 대칭)
- `build_posts.auto_logo(line1, line2)` → `assets/logos/brand_map.json` 을 보고
  카피에 기업명이 있으면 **자동 부착**. 기사 JSON에 logo를 안 써도 된다.
  긴 키워드 우선 매칭('삼성바이오로직스' > '삼성'). 오탐 없음(밥상 물가 → 없음).
  직접 지정하려면 "logo":"파일명" (tickers→logos 순으로 탐색, 없으면 경고 출력).
- 로고 라이브러리: assets/tickers/ 34종 + assets/logos/ 에 LG·POSCO·한국은행·
  하나금융·신한은행·한국거래소·삼성바이오로직스 추가(전부 Public domain, 육안 검증).
- ⛔ 로고는 상표다. 그 기업을 실제로 다루는 기사에만 쓴다.
- 새 로고 받을 때: `python _engine/fetch_logo.py "Company Name" --name slug`
  → Commons가 자회사·PDF 등 쓰레기를 반드시 섞어주므로 **Read로 눈으로 확인 후 선별**.

## ✅ 제목↔이미지 매칭 자기검증 — 도입 완료 (2026-08-03)
문제였던 것: photo_category 안에서 '순환'으로만 골라서 돌려쓰기는 막았지만
'이 기사에 맞는가'는 아무도 안 봤다 → 카테고리를 잘못 찍으면 엉뚱한 사진이 조용히 나감.

`_engine/photo_match.py` 3단 구조:
1. **채점** — 사진 메타(검색어·원제·카테고리·용도)와 기사(제목+썸네일 카피)를
   한글 개념사전(CONCEPTS)으로 대조. 한글 직접 일치 +3.0 > 영문 유의어 +1.5.
   ※ 한글 일치를 안 보면 `용도: 수출·무역·물류` 같은 한글 설명만 있는
     port_terminal.jpg 이 저점을 받아 오탐이 난다(실제로 겪음).
2. **자동교체** — 적합도 0(접점 전무) + 확실한 대안(≥STRONG_SCORE 8.0)일 때만
   갈아끼운다. 애매하면 경고만 → 작성자의 의도적 선택을 망가뜨리지 않기 위함.
3. **육안검수** — `python _engine/contact_sheet.py {MMDD}` →
   `output/{MMDD}/_검수시트.jpg` (썸네일 격자 + 제목 + 경고표시).
   점수로 못 잡는 것(폭락인데 톤 밝음, 카피 안 읽힘, 로고 엉뚱)은 결국 봐야 안다.
   10건을 **이미지 1장**으로 보므로 자동 루틴이 발행 전 스스로 검수하기 가장 싸다.

- photo/photo_category 둘 다 없으면 제목을 읽고 자동 선택한다(= 카테고리 안 찍어도 됨).
- 등락 단어(폭락/급등/신고가…) + 사진 썸네일 → stock_thumbnail 권고를 problems 에 넣는다.
- 검증 결과는 전부 `build_report.json` 의 problems 로 올라간다.
- 루틴 프롬프트 7번 단계에 (a)리포트 확인 (b)검수시트 육안확인이 필수로 박혀 있다.
- 개념 사전을 늘리려면 photo_match.CONCEPTS 에 '한글키: [영문 유의어...]' 추가.

## 🔧 Wikimedia 429의 진짜 원인 = User-Agent (2026-08-03, 삽질 방지)
`HTTP 429 Too many requests` 가 떠서 rate limit 으로 보이지만, 실제로는 Wikimedia의
UA 정책 위반이었다. 제품명+버전+**연락처 URL**이 없는 UA는 이 에러로 막는다.
기다려도 안 풀리고 UA만 고치면 즉시 원본까지 받아진다. `image_sourcing.UA` 참고.
`fetch_image()` 에 429/503 백오프도 추가했다(`_get`에만 있었음).

## ✅ 사진 다양성 시스템 — 도입 완료 (2026-08-03, "같은 사진 중복 최악")
장수만 늘리면 안 풀리는 문제였다. 네 갈래로 나눠 처리했다.

**1) 회전 로직이 진짜 병목이었다** (`photo_library.recent_window`)
   예전엔 `RECENT_WINDOW = 3` 고정. 30일 시뮬레이션 결과:
     풀 20장 / 기존(3고정) → 최단 재등장 4일, 20장 중 15종만 사용
     풀 20장 / 개선(70%)  → 최단 재등장 15일, 20종 전부 사용
   즉 **소싱만 늘렸으면 4일마다 같은 사진이 나왔다.** 풀의 70%를 최근분으로
   배제하도록 바꿔서 한 바퀴를 거의 다 돌아야 재등장한다.

**2) 검색어를 카테고리당 2개 → 6~8개로** (`build_stock_library.TOPICS`)
   같은 검색어로 20장 받으면 비슷한 아파트 20장이다. 장면을 쪼개야 진짜 다양해진다:
   부동산 = [스카이라인 / 공사현장 / 중개소 / 열쇠·계약 / 이사 / 항공뷰].
   사람이 나오는 장면(계약·상담·장보기)은 감정 전달이 좋아 홈판에서 강하다.

**3) 픽셀 기준 중복 판정** (`photo_quality.py`)
   파일명이 다르면 순환 로직은 '다른 사진'으로 착각한다 → dhash(구조)로 비교.
   ⚠ dhash 단독은 오탐이 난다 — 한강 다리와 실리콘 웨이퍼가 거리 9로 잡혔다
     (둘 다 '반복되는 가로 띠' 구조). 그래서 **색 분포까지 같아야** 중복 확정.
     이 검증으로 오탐 14쌍 → 5쌍으로 줄었다.
   자동삭제는 거리 4 이하만(확실한 것만). 애매한 건 사람이 본다.
   소싱 단계에도 게이트를 걸어 중복이 애초에 안 쌓인다.

**4) 밝기 기반 자동 dim** (`photo_quality.suggest_dim`)
   썸네일 카피는 항상 흰색이라 배경이 밝으면 안 읽힌다. 사진 실제 밝기를 재서
   최소 dim을 자동 보장한다(기사가 더 큰 값을 주면 그쪽 존중 — 더 어둡게만 허용).

### ⚠️ 이 작업에서 데이터를 날린 함정 2가지 (반드시 기억)
- **파일명 충돌**: 새 소싱 접두어를 `{cat}_{qi}`로 쓰면 검색어를 늘렸을 때
  기존 파일(`물가소비_0_pexels_0.jpg`)을 덮어쓴다. 그러면 중복 게이트가
  '자기 자신과 중복'으로 보고 삭제해 라이브러리가 줄어든다.
  → `next_prefix_index()` 로 안 쓰인 번호부터 시작. 자기중복이면 삭제 금지(이중 안전장치).
- **index/디스크 desync**: index.json 은 git 추적, 사진은 .gitignore.
  `git checkout -- assets/photos/` 하면 인덱스만 과거로 가서 사진 77장이
  통째로 사라진 것처럼 된다. → `python _engine/photo_quality.py reindex` 로 복구.

### 운영 명령
```
python _engine/photo_quality.py audit          # 중복·밝기 진단
python _engine/photo_quality.py audit --fix    # 확실한 중복만 삭제
python _engine/photo_quality.py reindex        # 디스크에만 있는 사진 복구
python _engine/build_stock_library.py --per 14 # 전체 보강(카테고리 지정 가능)
```

(위 '사진 다양성 시스템'으로 해결됨 — 남은 보조 수단: 종목 기사는 stock_thumbnail
비중을 늘리면 매번 다른 종목·가격·차트라 애초에 안 겹친다. variation_seed 의
크롭·톤 변주는 이미 적용 중이라 같은 원본도 날짜·순번마다 다르게 보인다.)

## 2026-08-02 두 번째 자동 실행 (추가 발행 + git 동기화 이슈 발견/수정)
- 같은 날 두 번째 루틴 실행에서 output/0802에 이미 1~8이 있는 걸 확인 →
  덮어쓰지 않고 9~12로 이어붙임(주식증시/가상자산/글로벌경제/금리통화 4건).
  build_report.json problems 0건. seen_articles.json 갱신, push 완료.
- ⚠️ 중요: 세션 시작 시 로컬 `main`과 `origin/main`이 전부 최초 커밋(4063bbd,
  "initial: v1.0 prompt...")에 멈춰 있었고, 실제 하루치 작업(8건 발행,
  이미지 파이프라인 수정, v2/v3 샘플, 핸드오프 문서 등 45개 커밋)은 전부
  detached HEAD 상태로만 존재 — 즉 그동안 원격에 실제로 push된 적이 없었던
  것으로 보인다(SYSTEM_STATE 이전 기록의 "push 성공"은 detached HEAD 로컬
  커밋 성공을 오인한 것일 가능성). `git branch -f main HEAD` 로 main을
  안전하게 fast-forward(45커밋 모두 main의 조상 관계 확인 후)한 뒤 push해서
  해결 — origin/main도 이제 정상 동기화됨. 다음 세션은 시작 시
  `git branch -vv`로 detached HEAD 여부를 먼저 확인할 것.
- 헤드리스 브라우저(SVG→PNG 변환)가 이 실행 환경에선 PATH에 없어서 렌더가
  전부 스킵됐었다 → `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`를
  `/usr/local/bin/chromium`에 symlink해서 해결(세션 한정, 컨테이너 재시작 시
  다시 필요할 수 있음). PIL(pillow)도 미설치 상태라 `pip install pillow` 필요했음.
- output/0802/{9~12}는 build_posts.py main()을 그대로 쓰면 1번부터 덮어쓰므로,
  build_one()을 직접 호출해 시작 인덱스를 9로 지정하는 임시 드라이버 스크립트를
  scratchpad에 만들어 실행함(_engine 코드는 손대지 않음).

## 그 다음 할 일
1. [사용자] 오늘 output/0802/1~8 중 하나를 네이버에 실제 발행 → 홈판 노출·저품질 검증
2. [사용자] output/samples/0802_v2/1~3 톤 피드백 → v2 채택 여부 결정.
   채택되면: PROMPT_V1.md에 페르소나·감정로테이션·계급서열 시리즈 절 정식 반영,
   루틴 프롬프트도 v2 기준으로 갱신, 실제 발행 파이프라인을 5장 이미지로 전환.
3. 사진 다양성 보완 (위 3가지)
4. 검증되면 config.json으로 주제설정 중앙화 → 자동차/건강 복제 (GitHub 링크 공유로 확장)
